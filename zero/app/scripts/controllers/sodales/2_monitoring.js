angular.module('mqnaasApp')
    .controller('SodalesMonitoringController', function ($scope, $filter, localStorageService, $modal, arnService, cpeService, $interval) {
        var promise;
        var availableResources = [];
        $scope.selected = "";

        //getMqnaas model. For each ARN and CPE, add to available Resources array




    });
